# docs
Repository uses for holding the various versions of the documentation and book of ariel-os

## Structure 

The [tags](./tags) directory hold the versionned docs for each of our releases.
Each `tags/x/` directory is divided between a `book/` subdirectory that hold the book and a `api/` directory that holds the `rustdoc` documentation. 
